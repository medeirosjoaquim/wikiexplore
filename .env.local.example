# Local overrides - copy to .env.local for local-only tweaks
APP_ENV=local

# Host-accessible endpoints for running scripts outside docker
POSTGRES_HOST=localhost
DATABASE_URL=postgresql+psycopg://wikipulse:wikipulse@localhost:5432/wikipulse
KAFKA_BOOTSTRAP_SERVERS=localhost:9094
ELASTICSEARCH_URL=http://localhost:9200
